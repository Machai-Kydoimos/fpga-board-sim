"""Embedded-core generator package (CPU-on-board single-file VHDL systems).

Currently holds the vendored CPU core(s) under ``cores/`` and the ROM-image
helper; the full generator (``gen_embedded_core.py``) lands in Stage 3.
"""
